{
  "name": "leadflow-root",
  "version": "1.0.0",
  "private": true,
  "description": "LeadFlow - Omnichannel lead generation and workflow automation platform",
  "scripts": {
    "server:dev": "npm --prefix server run dev",
    "client:dev": "npm --prefix client run dev"
  }
}
